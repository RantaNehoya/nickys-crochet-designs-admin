name: nickys_crochet_designs
description: nickys crochet design admin panel.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.5.3 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  flutter_web_data_table: ^1.0.0
  font_awesome_flutter: ^9.2.0
  provider: ^6.0.1
  firebase_core: ^1.8.0
  firebase_auth: ^3.1.4
  cloud_firestore: ^2.5.4
  image_picker: ^0.8.4+1
  badges: ^2.0.1
  intl: ^0.17.0
  google_fonts: ^2.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.4

flutter:

  uses-material-design: true

  assets:
    - assets/images/
